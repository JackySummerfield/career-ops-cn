interface:
  display_name: "Career Ops CN"
  short_description: "Fetch and evaluate JDs, tailor resumes, and prepare interviews"
  default_prompt: "Use $career-ops-cn to fetch and evaluate this job posting, then show the fit and next action. Before reading or writing data, use the explicitly configured local data root or the GitHub repository allowed by private runtime configuration; never access an unapproved data repository or the public program repository for personal data."
